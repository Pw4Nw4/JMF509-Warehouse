# PostgREST configuration for JMF 509 Warehouse
# Used when running PostgREST via Docker or standalone

db-uri = "postgres://postgres:postgres@db:5432/jmf509_warehouse"
db-schemas = "public"
db-anon-role = "postgres"
db-pool = 10
server-port = 3000
server-host = "0.0.0.0"
