# Docker stack - PostgreSQL only (PostgREST/pgAdmin run separately)
# Copy to .env and set values, or run: ./setup-docker-env.sh to auto-generate

POSTGRES_USER=jmf509_user
POSTGRES_PASSWORD=CHANGE_ME_use_strong_password
POSTGRES_DB=jmf509_warehouse
